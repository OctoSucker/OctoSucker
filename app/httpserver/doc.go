// Package httpserver serves the web admin UI (static shell + JSON APIs).
// It is dependency-injected from app so HTTP concerns stay isolated and routes can grow without bloating app.
package httpserver
